# Example prompt

```
Render nimbus-launch.md to claudecode-deck, branded as Nimbus Studio.
```

This shows the full `claudecode-deck` component catalog — every slide class
(cover, bigstat, pillars, models, glossary, filegrid, featurepair, compare3,
split, steps, iconcards, stack, quote, statement, closing) exercised once, with
hand-written transparent-ground SVG figures and clay-stroke icons sitting
directly on the cream wash.
